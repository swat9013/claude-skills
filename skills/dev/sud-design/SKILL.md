---
name: sud-design
user-invocable: true
argument-hint: "[設計名または設計したい対象の説明]"
description: >-
  設計ドキュメント一式をシステム関連図 → ユースケース → ドメインモデル図の順の
  対話的セッションで作成・更新する。モジュール分割・実装アーキテクチャの設計は対象外。
  Use when「設計ドキュメント (設計 doc) を作る・更新する」「ドメインモデルを設計する」
  「ユースケースを書く」「システム境界を設計する」.
---

# sud-design

設計対象について、システム関連図 (境界と接続点) → ユースケース (アクターの意図) → ドメインモデル図 (実体と制約) の順に質問して決定を固め、設計ドキュメント一式を書く。順序が漏れを炙り出す: 境界を決めずに描いたドメインモデルは port の内外が混ざり、ユースケースに現れない entity は根拠を持たない。

成果物の書式はすべて [references/artifacts.md](references/artifacts.md)、検証の手順は [references/verification.md](references/verification.md) が持つ。書く段階になったら該当ファイルを Read する。

## 1. 置き場を決める

- 既定は `docs/design/<name>/`。対象リポジトリに設計ドキュメントの置き場が既にあるならそちらに従う
- `<name>` は設計対象の名前 (kebab-case)。既存ディレクトリと重複したら、それが同じ対象の旧設計か別対象かをユーザーに確かめる — 同じ対象なら「4. 既存設計を更新する」の側

## 2. 設計セッションを進める

各段は、下記の確定項目が埋まるまで質問を続けてから次の段へ進む。質問はまとめて出し、ユーザーの回答が要らない事実 (既存コード・既存 doc の実態) は自分で調べて埋める。決定するのはユーザー、事実を集めるのは自分。

**システム関連図** — 確定項目:

- システム境界: 何が中で誰が外か。境界の置き方の理由
- 相手ごとの接続: 経由 (port か否か) と方向。port の切り方は変動性 (取り替えの起きやすさ) で決める
- 状態機械の有無と、あるなら状態・イベントの一覧

**ユースケース** — 確定項目:

- 外部アクター全員について、user-goal レベルのユースケースが列挙されている (アクターが登場しないユースケース・ユースケースに現れないアクターが残っていない)
- 各ユースケースの Main Success Scenario と主要な Extensions

**ドメインモデル図** — 確定項目:

- entity・集約の範囲・多重度・ID 参照。ユースケースに現れない entity は理由を確かめる (根拠が無ければ落とす)
- entity ごとの不変条件 (note に束縛する制約)
- 用語: 図に登場する語は全部、用語の正本 (既存の用語集、無ければ README の対訳表) と一致させる

system.md / usecases.md / domain.puml の 3 成果物は既定で全部作る。設計の規模に対して不要な成果物は省いてよいが、**何を省きなぜ不要かを README に 1 行残す** (省略の判断も設計判断として読める形にする)。

## 3. 書く・検証する・描く

1. [references/artifacts.md](references/artifacts.md) を Read し、確定した内容を各成果物へ書く
2. [references/verification.md](references/verification.md) を Read し、検証する:
   - 遷移列シナリオ 2〜3 本 (必須)。表現できない点が出たらモデル側を直す
   - 状態機械を持つ設計は `uv run ${CLAUDE_SKILL_DIR}/scripts/check-statechart.py <statechart.puml>` が exit 0 になるまで、遷移の追加か理由つき uncovered 宣言で埋める
3. `.puml` を描画する。対象リポジトリに PlantUML の描画 gate (再描画コマンド) があればその生成側を使い、無ければ:

   ```bash
   uv run ${CLAUDE_SKILL_DIR}/scripts/render-puml.py docs/design/<name>/
   ```

   `.svg` は `.puml` と必ず対で更新する (source だけ直すと README が古い図を貼り続ける)
4. セッション中の決定のうち根拠と却下肢を残す価値があるものを `decision/NNNN-<slug>.md` に記録する (基準と様式は artifacts.md)

## 4. 既存設計を更新する

設計済みの対象に構造変更 (境界・ユースケース・entity・多重度・状態機械の変更) が入るときは、**設計ドキュメントを先に直してから実装する**。実装が先に着地すると、設計 doc は実装の後追い要約に落ちて正本でなくなる。

- どの変更がどの file の正本かは README の索引表で引く
- `domain.puml` / `statechart.puml` を直したら再描画と組合せ検査を回す (手順は「3.」と同じ)
- 過去の決定を覆すときは decision/ の旧 file を書き換えず、新しい決定 file を起こして旧 file を Superseded にする

## 何をしない skill か

- **実装しない**: 設計ドキュメントの完成が終点。実装は呼び出し元のフローに委ねる
- **リポジトリ全体の ADR を起票しない**: decision/ からの昇格基準 (artifacts.md) に当たる決定は、対象リポジトリの ADR 運用に従うようユーザーに提案する
- **モジュール分割・実装アーキテクチャは扱わない**: 本 skill の対象はドメインの構造 (境界・ユースケース・実体) まで
- **更新の enforce は生成しない**: 「doc を先に直す」を機械的に守らせる仕組み (rules / hook) を作るかは対象リポジトリの判断
